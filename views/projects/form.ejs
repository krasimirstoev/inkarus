
<div class="row justify-content-center">
  <div class="col-md-8">
    <h2 class="mb-4 text-center"><%= __('Projects.Form.new_title') %></h2>

    <form action="/projects" method="POST">
      <div class="mb-3">
        <label for="title" class="form-label"><%= __('Projects.Form.title_label') %></label>
        <input type="text" class="form-control" id="title" name="title" required>
      </div>

      <div class="mb-3">
        <label for="description" class="form-label"><%= __('Projects.Form.description_label') %></label>
        <textarea class="form-control" id="description" name="description" rows="3"></textarea>
      </div>

      <button type="submit" class="btn btn-primary w-100"><%= __('Projects.Form.create_button') %></button>
    </form>
  </div>
</div>
